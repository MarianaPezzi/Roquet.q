export declare function isBrowser(): boolean;
/**
 * Runs the given function and logs the given message when a `MODULE_NOT_FOUND` is
 * encountered to make loading of optional dependencies less verbose.
 *
 * @param fn A function that `require()`s a optional dependency
 * @param failureMessage Message logged when the module is missing
 */
export declare function tryLoading<T>(fn: () => T, failureMessage: string): T;
