import { NumericIdentifier } from './identifier';
import { URLString } from './string-fields';
interface PhotoVersionFields {
    url: URLString;
    width: number;
    height: number;
}
export interface PhotoVersion extends PhotoVersionFields {
    crop: boolean;
    name: string;
}
export interface Photo extends PhotoVersionFields {
    versions: PhotoVersion[];
    media_type: string;
    id: NumericIdentifier;
}
export declare function toPhoto(data: any): Photo;
export {};
