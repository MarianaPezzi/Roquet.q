"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseTimestamp = void 0;
const hasTimezoneRegex = /(\+\d+(:\d+)*|Z)$/;
/**
 * Takes a timestamp from the API and returns a new Date
 * @param timestamp as string
 * @returns {Date} of the timestamp
 */
function parseTimestamp(timestamp) {
    // Beekeeper API dates should be considered UTC if no timezone is given
    const dateWithTimezone = `${timestamp}${timestamp.match(hasTimezoneRegex) ? '' : 'Z'}`;
    return new Date(dateWithTimezone);
}
exports.parseTimestamp = parseTimestamp;
