import { Conversation } from './conversation';
import { NumericIdentifier, UsernameIdentifier, UUIDIdentifier } from './identifier';
import { Profile } from './profile';
/** Enum for the different types of message types */
export declare enum MessageTypes {
    REGULAR = "regular"
}
/**
 * An interface for a message
 */
export interface Message {
    text: string;
    id?: NumericIdentifier;
    message_type: MessageTypes;
    conversation_id: NumericIdentifier;
    user_id: UUIDIdentifier;
    uuid?: UUIDIdentifier;
    profile?: UsernameIdentifier;
}
export interface MessageData {
    text: string;
}
export declare function messageFactory(sender: Profile, conversation: Conversation, messageData: MessageData): Message;
export declare function toMessage(data: any): Message;
