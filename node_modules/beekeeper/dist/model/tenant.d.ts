import { NumericIdentifier, UUIDIdentifier } from './identifier';
import { URLString } from './string-fields';
/**
 * A tenant interface mainly used by the client endpoint
 */
export interface Tenant {
    max_file_size: number;
    feature_flags: {
        [key: string]: boolean;
    };
    styling: object;
    max_files_on_post: number;
    max_video_size_for_admin?: number;
    general: {
        company_account: UUIDIdentifier;
        name: string;
        language: string;
        url: URLString;
        tagline?: string;
        id: NumericIdentifier;
        subdomain: string;
        support_email?: string;
    };
    integrations: {
        pubnub: {
            subscribe_key: string;
        };
    };
}
export declare function toTenant(data: any): Tenant;
