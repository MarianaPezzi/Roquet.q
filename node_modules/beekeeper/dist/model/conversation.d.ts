import { NumericIdentifier, UsernameIdentifier } from './identifier';
import { Message } from './message';
export declare enum ConversationType {
    ONE_ON_ONE = "one_on_one",
    CAMPAIGN = "campaign",
    GROUP = "group",
    SURVEY = "survey"
}
export declare enum Folder {
    INBOX = "inbox",
    ARCHIVE = "archive"
}
export interface Conversation {
    replied?: boolean;
    conversation_type: ConversationType;
    features?: object;
    is_admin?: boolean;
    muted_until?: object;
    id: NumericIdentifier;
    name: string;
    usernames?: UsernameIdentifier[];
    muted: boolean;
    modified: Date;
    snippet?: string;
    avatar: string;
    profile?: UsernameIdentifier;
    folder: Folder;
    is_unread: boolean;
    messages?: Message[];
}
export declare function toConversation(data: any): Conversation;
