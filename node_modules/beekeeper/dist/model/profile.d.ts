import { toUser, User } from './user';
export interface Profile extends User {
}
export interface ProfileContext {
    self?: {
        is_me: boolean;
        is_managed_by_me: boolean;
    };
    user: Profile;
}
export declare const toProfile: typeof toUser;
export declare function toProfileContext(data: any): ProfileContext;
