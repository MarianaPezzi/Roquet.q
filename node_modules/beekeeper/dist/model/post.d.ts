import { File } from './file';
import { NumericIdentifier, UUIDIdentifier } from './identifier';
import { Photo } from './photo';
import { Profile } from './profile';
import { Stream } from './stream';
import { Video } from './video';
export interface Post {
    id?: NumericIdentifier;
    text?: string;
    title: string;
    streamid: NumericIdentifier;
    user_id: UUIDIdentifier;
    files?: File[];
    locked?: boolean;
    labels?: string[];
    sticky?: boolean;
    media?: Array<Video | Photo>;
    scheduled_at?: Date;
}
export interface PostData {
    title: string;
    text?: string;
    labels?: string[];
}
export declare function postFactory(sender: Profile, stream: Stream, postData: PostData): Post;
export declare function toPost(data: any): Post;
