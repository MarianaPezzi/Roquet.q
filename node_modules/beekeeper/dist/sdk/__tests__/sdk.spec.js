"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const sdk_1 = require("../sdk");
describe('SDK', () => {
    const credentials = {
        tenantURL: 'https://tenant.beekeeper.io',
        token: 'token',
    };
    let sdk;
    beforeEach(() => {
        sdk = new sdk_1.SDKImpl(credentials);
    });
    describe('.Auth', () => {
        test('is defined', () => {
            expect(sdk.Auth).toBeDefined();
        });
    });
});
