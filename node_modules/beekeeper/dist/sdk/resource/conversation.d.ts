import { Conversation, Profile } from '../../model';
import { Resource } from './resource';
export interface ConversationResource {
    byProfile(profile: Profile): Promise<Conversation>;
}
export declare class ConversationResourceImpl extends Resource implements ConversationResource {
    byProfile(profile: Profile): Promise<Conversation>;
}
