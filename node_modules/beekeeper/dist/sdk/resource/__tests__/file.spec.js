"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const file_1 = require("../file");
describe('FileResource', () => {
    describe('makeURL()', () => {
        test('removes extra `/` characters from the url if they exist', () => {
            const resource = new file_1.FileResourceImpl('https://deniz.dev.beekeeper.io/');
            const expected = 'https://deniz.dev.beekeeper.io/api/2/files';
            const actual = resource.makeURL('/api/2/files');
            expect(actual).toBe(expected);
        });
        test('appends search parameters correctly', () => {
            const resource = new file_1.FileResourceImpl('https://deniz.dev.beekeeper.io/');
            const expected = 'https://deniz.dev.beekeeper.io/api/2/files?a=1&b=2&c=marvelous';
            const actual = resource.makeURL('/api/2/files', { "a": 1, "b": 2, "c": "marvelous" });
            expect(actual).toBe(expected);
        });
    });
});
