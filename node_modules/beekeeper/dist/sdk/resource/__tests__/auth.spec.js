"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const auth_1 = require("../auth");
const mockedFetch = window.fetch;
describe('AuthenticationResource', () => {
    const baseURL = 'https://tenant.beekeeper.io';
    let resource;
    beforeEach(() => {
        resource = new auth_1.AuthenticationResourceImpl(baseURL);
    });
    describe('.generateJWT()', () => {
        test('calls /api/2/jwt endpoint', async () => {
            const expected = `${baseURL}/api/2/jwt`;
            resource.generateJWT();
            expect(window.fetch).toBeCalled();
            const [url] = mockedFetch.mock.calls[0];
            expect(url).toBe(expected);
        });
    });
});
