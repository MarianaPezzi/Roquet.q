import { Message } from '../../model';
import { Createable, Resource } from './resource';
export interface MessageResource extends Createable<Message> {
}
export declare class MessageResourceImpl extends Resource implements MessageResource {
    create(message: Message): Promise<Message>;
}
