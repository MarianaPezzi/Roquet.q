import { ClientConfig } from '../../model';
import { Getable, Resource } from './resource';
export interface ClientConfigResource extends Getable<ClientConfig> {
}
export declare class ClientConfigResourceImpl extends Resource implements ClientConfigResource {
    get(): Promise<ClientConfig>;
}
