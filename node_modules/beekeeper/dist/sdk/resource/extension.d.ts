import { Extension } from '../../model';
import { Browseable, Filter, Resource } from './resource';
export declare enum ExtensionsPlatform {
    IOS = "ios",
    ANDROID = "android",
    WEB = "web"
}
export declare class ExtensionsFilter implements Filter {
    platform?: ExtensionsPlatform;
    expandPlaceholders: boolean;
    redirectJwt?: boolean;
    limit?: number;
    /** @deprecated This is obsolete and is not used anymore */
    includeTrackingType: boolean;
    get query(): Record<string, any>;
}
export interface ExtensionResource extends Browseable<Extension> {
}
export declare class ExtensionResourceImpl extends Resource implements ExtensionResource {
    list(filter?: ExtensionsFilter): Promise<Extension[]>;
}
