export default class ResourceError extends Error {
    status: number;
    text: string;
    error?: string;
    constructor(status: number, text: string, error?: string);
    static fromResponse(response: Response): Promise<ResourceError>;
    private static getErrorBody;
}
