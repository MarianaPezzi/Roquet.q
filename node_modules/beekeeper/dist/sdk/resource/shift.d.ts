import { Shift } from '../../model';
import { Browseable, Resource, Filter } from './resource';
export declare class ShiftsFilter implements Filter {
    start?: Date;
    end?: Date;
    get query(): Record<string, any>;
}
export interface ShiftResource extends Browseable<Shift> {
}
export declare class ShiftResourceImpl extends Resource implements ShiftResource {
    list(filter?: ShiftsFilter): Promise<Shift[]>;
}
