import { NumericIdentifier } from '../../model';
import { Stream } from '../../model/stream';
import { Browseable, Resource } from './resource';
export interface StreamResource extends Browseable<Stream> {
    get(streamId: NumericIdentifier): Promise<Stream>;
}
export declare class StreamResourceImpl extends Resource implements StreamResource {
    /**
     * Fetches and returns a stream by NumericIdentifier
     * @param streamId an integer identifying a stream of a tenant
     */
    get(streamId: NumericIdentifier): Promise<Stream>;
    list(): Promise<Stream[]>;
}
