import { Profile, ProfileContext, UsernameIdentifier } from '../../model';
import { Browseable, Filter, Getable, Resource } from './resource';
export interface ProfileResource extends Getable<ProfileContext>, Browseable<Profile> {
}
export declare class ProfileResourceImpl extends Resource implements ProfileResource {
    get(identifier: UsernameIdentifier): Promise<ProfileContext>;
    /**
     * This endpoint is currently admin-only. In the future, we will provide a
     * method to access profiles by a normal user.
     */
    list(filter?: Filter): Promise<Profile[]>;
}
