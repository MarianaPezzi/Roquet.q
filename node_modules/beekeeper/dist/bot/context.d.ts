import { Message } from '../model';
import { SDK } from '../sdk';
export interface Context {
    /** Received message from sender */
    readonly originalMessage: Message;
    /**
     * Send a new message with text as content
     * @param {string} text to be send as content of the message
     * @returns {Promise<Message>} resolves with created message or rejects with error
     */
    replyWithText(text: string): Promise<Message>;
}
/**
 * A context implementation
 */
export declare class ContextImpl implements Context {
    readonly originalMessage: Message;
    private _sdk;
    /**
     * @param {Message} message originally received message
     * @param {SDK} sdk instace to be used for sending messages
     */
    constructor(message: Message, sdk: SDK);
    replyWithText(text: string): Promise<Message>;
}
