"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ContextImpl = void 0;
const model_1 = require("../model");
/**
 * A context implementation
 */
class ContextImpl {
    /**
     * @param {Message} message originally received message
     * @param {SDK} sdk instace to be used for sending messages
     */
    constructor(message, sdk) {
        this.originalMessage = message;
        this._sdk = sdk;
    }
    async replyWithText(text) {
        const user = await this._sdk.getMe();
        const reply = {
            text,
            conversation_id: this.originalMessage.conversation_id,
            message_type: model_1.MessageTypes.REGULAR,
            user_id: user.id,
        };
        return this._sdk.Messages.create(reply);
    }
}
exports.ContextImpl = ContextImpl;
